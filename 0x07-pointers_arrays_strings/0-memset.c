#include "main.h"
/**
 * *_memset- converts a string to an integer
 * @v: string to be converted
 * @j: jfjffkkfk
 * @n: intg
 * Return: the int converted from the string
 */

char *_memset(char *v, char j, unsigned int n)
{
unsigned int i;
for (i = 0; i < n; i++)
{
v[i] = j;
}
return (v);
}
