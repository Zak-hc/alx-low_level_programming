#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <unistd.h>
#include <string.h>
#include "lists.h"
/**
 * print_listint - Print_t list.
 * @h: A poit_t list
 * Return: The numnodes in
 */
size_t print_listint(const listint_t *h)
{
size_t count = 0;
while (h != NULL)
{
printf("%d", n);
count++
}
return (count);
}
